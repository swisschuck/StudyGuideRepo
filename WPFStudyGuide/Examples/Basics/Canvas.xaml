<UserControl x:Class="WPFStudyGuide.Examples.Basics.Canvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!--The Canvas is probably the simplest Panel of them all. It doesn't really do anything by default, it just allows you to put 
        controls in it and then position them yourself using explicit coordinates.-->
        <Canvas Height="300" Width="300">
            <Rectangle Canvas.Left="25" Canvas.Top="25" Width="50" Height="40" Fill="Red" />
            <Rectangle Canvas.Left="100" Canvas.Top="25" Width="50" Height="40" Fill="Orange" />
            <Rectangle Canvas.Left="125" Canvas.Top="40" Width="50" Height="40" Fill="Blue" />
            <Ellipse Canvas.Left="25" Canvas.Top="200" Width="50" Height="50" Fill="Green" />
        </Canvas>
    </Grid>
</UserControl>
